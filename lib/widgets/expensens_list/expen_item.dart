import 'package:flutter/material.dart';

class ExpenItem extends StatelessWidget {
  const ExpenItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}